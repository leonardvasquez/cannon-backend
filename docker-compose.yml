services:
  postgres:
    image: postgres:16-alpine
    container_name: cannon-postgres
    environment:
      POSTGRES_USER: cannon
      POSTGRES_PASSWORD: cannon123
      POSTGRES_DB: cannon_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: